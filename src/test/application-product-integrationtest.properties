#spring.application.name=qu1cksave Backend Tests
# Disable auto table-generation
spring.jpa.hibernate.ddl-auto=none
# Format for Data Source Url (using Postgres):
#   jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
#   Ex. jdbc:postgresql://localhost:5432/my_db_name
#spring.datasource.url=${POSTGRES_HOST}
#spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB} # BEFORE

# From https://www.baeldung.com/spring-boot-testcontainers-integration-test
#spring.datasource.url=jdbc:tc:postgresql:11.1:///integration-tests-db
spring.datasource.url=jdbc:tc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

# https://www.baeldung.com/spring-boot-testcontainers-integration-test
# IF NEEDED
#spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver
# TODO: (5/7/25) I'll need to set POSTGRES_DB to "test" instead of "dev"
# - Like in the SlugSell tests
# - Not sure where to do it though
# - Or maybe an alternative way would be better?
#   -- Could just hardcode it?